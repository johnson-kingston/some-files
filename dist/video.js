function videoplay(elementid) { 
    var element = document.getElementById(elementid);
	element.play(); 
} 
function videopause(elementid) { 
    var element = document.getElementById(elementid);
	element.pause(); 
} 